apiVersion: v1
kind: Service
metadata:
  name: {{ include "taiga.fullname" . }}-back
  labels:
    {{- include "taiga.labels" . | nindent 4 }}
    app: back
spec:
  type: {{ .Values.back.service.type | default "ClusterIP" }}
  {{- if .Values.back.service.port }}
  ports:
    - name: {{ .Values.back.service.name | default "http" }}
      port: {{ .Values.back.service.port }}
      targetPort: {{ .Values.back.service.targetport }}
      protocol: {{ .Values.back.service.protocol | default "TCP" }}
  {{- end }}
  selector:
    {{- include "taiga.selectorLabels" . | nindent 4 }}
    app: back
