apiVersion: v1
kind: Service
metadata:
  name: {{ include "taiga.fullname" . }}-back
  labels:
    {{- include "taiga.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.back.type | default "ClusterIP" }}
  {{- if .Values.service.back.port }}
  ports:
    - name: {{ .Values.service.back.name | default "http" }}
      port: {{ .Values.service.back.port }}
      targetPort: {{ .Values.service.back.targetport }}
      protocol: {{ .Values.service.back.protocol | default "TCP" }}
  {{- end }}
  selector:
    {{- include "taiga.selectorLabels" . | nindent 4 }}
