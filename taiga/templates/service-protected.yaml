apiVersion: v1
kind: Service
metadata:
  name: {{ include "taiga.fullname" . }}-protected
  labels:
    {{- include "taiga.labels" . | nindent 4 }}
    app: protected
spec:
  type: {{ .Values.protected.service.type | default "ClusterIP" }}
  {{- if .Values.protected.service.port }}
  ports:
    - name: {{ .Values.protected.service.name | default "http" }}
      port: {{ .Values.protected.service.port }}
      targetPort: {{ .Values.protected.service.targetport }}
      protocol: {{ .Values.protected.service.protocol | default "TCP" }}
  {{- end }}
  selector:
    {{- include "taiga.selectorLabels" . | nindent 4 }}
    app: protected
